#include "Base.xcconfig"

// 실제 AdMob ID 는 Secrets.xcconfig 에 둔다 (git 에 안 올라감). 없으면 테스트 ID 로 빌드된다.
//   ADMOB_APP_ID = ca-app-pub-...~...
//   ADMOB_BANNER_ID = ca-app-pub-.../...
//   ADMOB_INTERSTITIAL_ID = ca-app-pub-.../...
#include? "Secrets.xcconfig"
